apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arm64azdodockeragent.fullname" . }}-test-connection"
  labels:
    {{- include "arm64azdodockeragent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arm64azdodockeragent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
