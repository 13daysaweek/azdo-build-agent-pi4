apiVersion: v1
kind: Service
metadata:
  name: {{ include "arm64azdodockeragent.fullname" . }}
  labels:
    {{- include "arm64azdodockeragent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arm64azdodockeragent.selectorLabels" . | nindent 4 }}
